# 컴파운딩 엔지니어링: 작업의 학습을 영속 산출물로 환류하는 방법론

> **한 줄 요지** — 모든 작업이 끝날 때, 그 과정에서 얻은 학습을 *영속 산출물*(상시 지침·절차 문서·테스트·자동화 훅)에 환류해 다음 작업이 더 높은 기저선에서 출발하게 한다. 작업 세션의 맥락은 휘발성이라 그대로 두면 학습이 증발한다. **신호가 노이즈보다 빨리 쌓일 때만 복리(compounding)가 양수**가 된다.

이 문서는 AI 에이전트와의 엔지니어링 협업에서 **"방금 한 작업에서 배운 것으로 시스템을 스스로 개선"**시키는 일반 방법론을 정리한다. 도구·프로젝트에 종속되지 않는 원리만 다룬다.

---

## 1. 관점: 작업에는 3개의 차수(order)가 있다

| 차수 | 무엇 | 예 |
|---|---|---|
| **1차** | 일을 한다 | 기능 구현, 버그 수정 |
| **2차** | 일을 하는 *시스템*을 고친다 | 그 작업에 쓰는 절차·도구·지침을 개선 |
| **3차** | 시스템을 고치는 *과정*을 정한다 | ← 이 방법론 |

대부분의 협업은 1차에 머문다. 컴파운딩의 핵심은 **1차 작업의 잉여 산물(마찰·발견·판단)을 2차 산출물로 정주(定住)시키는 습관**이고, 그 습관을 표준화하는 것이 3차다. 3차를 한 번 정하면 이후 모든 작업이 자동으로 2차를 생산한다.

---

## 2. 수확 루프: 추출 → 정주 → 인코딩 → 가지치기

### ① 추출 — 무엇을 남길까 (노이즈 필터)
세 조건이 **모두** 서면 인코딩 후보. 안 서면 버린다.
- **재발성** — 다시 일어나는가? (1회성 우연·특정 환경의 수치 엣지는 제외)
- **비용** — 비자명했나 / 실제 시간을 잡아먹었나?
- **일반성** — 이 인스턴스를 넘어 일반화되는가?

> ⚠️ **과적합 경계.** 모든 것을 남기면 산출물이 오염된다. 한 번의 사건을 일반 규칙처럼 박지 말 것. 코드 리뷰에서 *수용/기각*을 가르는 그 절제를 학습 선별에도 똑같이 적용한다.

### ② 정주 — 어디에 둘까 (SSoT 라우팅)
학습 유형을 **회수 트리거가 맞는 단일 거처**에 매핑한다. 거처가 회수 시점과 어긋나면 죽은 지식이 된다.

| 학습 유형 | 거처(기능으로 기술) | 회수 시점 |
|---|---|---|
| 항상 지켜야 할 불변식 | 해당 스코프의 **상시 지침**(에이전트 지침 파일) | 그 스코프 작업 시 상시 |
| "X 하는 법" 절차·함정 | **절차 문서 / 스킬** | X 발동 시 |
| 특정 버그 / 회귀 | **테스트** | 매 빌드 (실행됨) |
| 결정의 *이유* | **결정 기록**(ADR 등) | 역설계 방지용 조회 |
| 개인 선호 · 작업방식 | **영속 메모리** | 관련성 리콜 |
| 상시 필요한 프로젝트 컨텍스트 | **상시 로드 지침** | 매 세션 |

> **원칙: 거처는 하나, 중복 금지. 더 좋은 거처가 생기면 *이전*한다.**
> 한 사실을 임시 메모리에 먼저 남겼다가, 더 적합한 절차 문서가 그것을 정식 기록하게 되면 **임시 메모리를 삭제**한다. 같은 사실이 두 곳에 있으면 둘 다 썩는다.

### ③ 인코딩 — 어떻게 쓸까
결론만 박지 말고 **트리거 + 행동 + 이유**를 함께 적는다.
> `"<증상>을 보면 → <행동>하라 → 왜냐면 <실패 모드>."`
> (예: "같은 입력에 결과가 비결정적이면 → 시드/순서를 고정하라 → 숨은 외부 상태가 레이스로 끼어드니까.")

이유를 남겨야 **미래의 내가 맥락이 변했을 때 재판단**할 수 있다(카고컬트 방지). 최소·스키머블하게.

### ④ 가지치기 — 복리의 전제
주기적으로 산출물을 훑어 **오래되거나 틀린 규칙을 삭제**한다. 적고 강한 규칙 > 많고 약한 규칙. 인코딩과 삭제는 한 쌍이다 — 삭제 없는 인코딩은 부채다.

---

## 3. 복리 가치의 위계 — 가능한 한 '실행 가능한' 층으로 내려라

| 층 | 효과 | 형태 |
|---|---|---|
| 낮음 — **사실/노트** | 재발견을 막음 | 함정 메모 |
| 중간 — **규칙/절차** | 접근을 표준화 | 체크리스트·절차 단계 |
| 높음 — **실행 산출물** | 일 자체를 없앰 | 테스트(자동 검증) · 스크립트(자동 수행) · **훅**(자동 발동) |

예컨대 *고정 간격 폴링*을 *이벤트 발생 시에만 깨우는 결정론적 스크립트*로 바꾸면, 그것은 *사실*이 아니라 **메커니즘**을 인코딩한 것이라 복리가 크다. 인코딩할 때 항상 묻는다: **"이 학습을 더 실행 가능한 층으로 내릴 수 있나?"**

---

## 4. AI에게 요청하는 프롬프트 패턴

### (a) 판단 게이트가 있는 수확 요청 — "반영해"보다 한 단계 위
> "이번 작업의 학습 후보를 ① 재발성·비용·일반성 ② 거처 ③ 인코딩 문구로 **먼저 제안**하라. 내가 고른 것만 영속화하라."

→ 바로 쓰지 않고 *제안 → 승인*을 끼워 과적합을 차단한다.

### (b) 역설계 프로브 — 무엇이 학습인지 모를 때
> "이 작업에서 **시간을 잡아먹은 지점**은 어디였고, 그게 **이미 어떤 산출물에 있었다면 사소해졌을까**?"

→ 마찰 지점이 곧 인코딩 후보다.

### (c) 메커니즘화 압박
> "이 학습을 **가장 실행 가능한 층**(테스트/스크립트/훅)으로 인코딩할 수 있나? 안 되면 왜 절차/노트에 머무는가?"

### (d) 검증 선행
> "인코딩 전에 그 학습이 **실제로 옳음을 증명**했나? 추측이면 인코딩하지 말고 먼저 검증하라."

→ 한 메커니즘을 절차에 박기 전에 실제 동작을 끝까지 검증한 *뒤에* 인코딩한다. 증명한 것만 남긴다.

---

## 5. 바로 쓰는 프롬프트 예시 (복붙용)

§4의 패턴을 작업 흐름에 그대로 붙여 쓸 수 있게 옮긴 템플릿. `<…>`만 채운다.

### 예시 1 — 작업 종료 시 전체 수확 (판단 게이트 포함) · *가장 자주 씀*
> 언제: 본질적 작업을 끝낸 직후, 무엇을 남길지 통째로 맡길 때.
```
방금 작업에서 얻은 '재사용 가능한 학습'을 수확하자. 바로 반영하지 말고 먼저 후보 목록으로 제안하라. 각 후보에:
1) 한 줄 요약
2) 추출 판정 — 재발성·비용·일반성 3조건 충족 여부 (하나라도 안 서면 "버림")
3) 거처 — 어느 영속 산출물(상시 지침 / 절차·스킬 / 테스트 / 결정기록 / 메모리)에 둘지와 이유
4) 인코딩 문구 — "<증상>을 보면 → <행동>하라 → 왜냐면 <실패모드>"
기존 산출물과 중복되는지도 표시하라. 내가 고른 항목만 영속화한다.
```

### 예시 2 — 마찰에서 학습 역설계
> 언제: 작업은 됐는데 무엇이 학습인지 모를 때.
```
이번 작업에서 '시간을 잡아먹었거나 비자명했던 지점'을 3개 이내로 짚어라. 각각에 대해, 그게 '이미 어떤 영속 산출물에 적혀 있었다면 사소해졌을지'를 적고, 그 산출물에 들어갈 한 줄 규칙(트리거 + 행동 + 이유)을 제안하라.
```

### 예시 3 — 메커니즘화 압박
> 언제: 학습이 "조심하라" 수준의 노트에 머물 때.
```
이 학습을 '가장 실행 가능한 층'으로 인코딩하는 안을 제시하라. 테스트(자동 검증) > 스크립트/자동화(자동 수행) > 훅(자동 발동) > 절차 문서 > 단순 노트 순으로 가능성을 따져, 더 낮은(실행 가능한) 층이 가능하면 그것을 택하고, 불가하면 왜 위 층에 머무는지 이유를 달아라.
```

### 예시 4 — 가지치기 (유지보수)
> 언제: 산출물이 쌓여 신호가 흐려질 때.
```
<대상 산출물/디렉터리>를 훑어, 오래됐거나 틀렸거나 더 이상 맞지 않는 항목을 식별하라. 각각 유지 / 갱신 / 삭제 중 하나를 근거와 함께 제안하라. '적고 강한 규칙'을 기준으로, 약하거나 중복인 항목은 삭제 후보로 올려라.
```

### 예시 5 — 자동화 정착 (메타-무브, 1회성)
> 언제: 수확을 매번 시키기 싫을 때 — 한 번만 박는다.
```
앞으로 본질적 작업이 끝날 때마다 위 수확 루프(후보 제안 → 내 승인 → 영속화)를 네가 스스로 먼저 제안하게 만들고 싶다. 이를 상시 로드 지침이나 작업-종료 훅으로 정착시키는 안을 제시하고, 내가 승인하면 적용하라.
```

---

## 6. 메타-무브: 방법론 자체를 인코딩하라

가장 큰 복리는 **이 수확 루프를 매번 설명하지 않게 만드는 것**이다.
- **상시 로드 지침**에 "본질적 작업 종료 시 §2·§4 수확 루프를 *스스로 제안*하라"를 한 줄 박으면, 다음 작업부터 에이전트가 알아서 후보를 올린다.
- **작업 종료 훅**(완료 시 자동 발동)으로 "학습 수확 제안"을 트리거하면 — 수확 자체가 메커니즘(위계 최상단)이 된다.
- 혹은 **전용 명령/스킬**로 절차화한다.

3차를 한 번 인코딩하면 2차 생산이 자동화된다.

---

## 7. 가드레일 요약
1. **과적합 차단** — 재발성·비용·일반성 3조건 + 제안/승인 게이트.
2. **검증 선행** — 증명한 것만 인코딩(추측 금지).
3. **SSoT·이전** — 거처 하나, 중복 금지, 더 나은 거처 생기면 이전.
4. **가지치기** — 인코딩과 삭제는 한 쌍. 신호 > 노이즈일 때만 복리.
5. **이유 보존** — 트리거+행동+이유로, 재판단 가능하게.

---

## 부록 A — 일반화된 워크드 예시

추상 시나리오로 본 한 사이클. (특정 도구·프로젝트가 아니라 *형태*를 본다.)

| 마찰/발견 (1차에서) | 추출 판정 | 거처 (2차) | 인코딩 형태 |
|---|---|---|---|
| 고정 간격 폴링이 비효율(대부분 빈 확인) | 재발·비용·일반 ✓ | 절차/자동화 | 이벤트 발생 시에만 깨우는 결정론적 스크립트 *(메커니즘)* |
| 비결정적 신호 판정에 불안정한 식별자 사용(외부가 식별자를 재배치) | ✓ | 절차 규칙 | "안정적 카운트·타임스탬프로만 판정" |
| 도구의 비자명한 함정(직접 주입한 상태가 내부 로직에 덮임) | ✓ | 절차 함정(gotcha) | "내부 동작 경로로 상태에 도달하라" + 이유 |
| 환경 복원이 특정 전제 충족분만 동작 | ✓ | 셋업 레시피 | 시드 전제조건 명시 |
| 특정 수치/환경에서만 나타나는 1회성 엣지 | 일반성 ✗ | — | **인코딩 안 함** |
| 같은 사실이 두 거처에 중복 | 거처 개선 | 더 적합한 곳으로 이전 | 중복 삭제 |

→ 핵심 패턴: **마찰 → 검증 → 메커니즘/규칙으로 인코딩 → 올바른 SSoT에 정주 → 중복 제거.**
