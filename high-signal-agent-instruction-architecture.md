# 고신호 에이전트 지침 설계: 문서가 아니라 런타임 컨텍스트로 다루기

> **한 줄 요지** — 계층형 에이전트 지침은 설명서가 아니라, 다음 탐색 위치와 위반하면 안 되는 경계를 결정하는 **상시 로드 컨텍스트 라우터**다. 길이 자체가 아니라 상시 주입·탐색·위반·재작업·유지보수의 총비용을 최소화해야 한다.

## 1. 지침의 역할은 두 가지면 충분하다

일반 문서는 필요할 때 찾아 읽지만, 상시 지침은 관련 스코프에 들어갈 때마다 비용을 낸다. 따라서 다음 두 역할만 맡긴다.

1. **탐색 공간 축소** — 어느 책임 영역·디렉터리로 가야 하는가.
2. **경계 보존** — 코드를 부분적으로 읽으면 놓치기 쉬운 소유권·의존 방향·신뢰·승인 불변식은 무엇인가.

기능 설명, 구현 목록, 작업 명령, 변경 이력까지 담으면 지침은 축소판 문서 포털이 된다. 그때부터 탐색을 돕기보다 오래된 사전지식(stale prior)을 반복 주입한다.

## 2. 위험도 기반 보존 게이트

문장은 상시 주입 비용보다 **미리 아는 기대편익**이 클 때 남긴다.

```text
기대편익 = 탐색 절감 + (발견 지연 가능성 × 위반 비용)
총비용   = 반복 주입 비용 + drift·유지보수 비용
```

판단할 때는 다음 축을 함께 본다.

| 축 | 확인할 질문 |
|---|---|
| **선행 필요성** | 작업이나 수정 전에 알아야 하는가 |
| **발견 지연** | 일반 탐색이 올바른 시점까지 이를 드러내지 못할 가능성이 큰가 |
| **위반 비용** | 놓치면 보안, 데이터, 배포, 승인, 소유권 오류가 커지는가 |
| **안정성과 재사용** | 구현이 변해도 유효하고 해당 스코프에서 반복되는가 |
| **대체 수단** | 자동화나 온디맨드 라우팅이 더 싸고 확실한가 |

모든 축을 같은 비중의 AND 조건으로 다루지 않는다. 드물게 발생해도 위반 비용이 매우 크거나 발견이 너무 늦는 운영 불변식은 상시 지침에 남길 수 있다.

남길 가능성이 높은 것은 최종 소유자와 비소유 영역, 의존 방향, 신뢰·민감정보 경계, 인간 승인 게이트, 생성물의 작성 원본, 필수 release gate, 여러 구현을 관통하는 수명 의미다. 파일·함수·API 전체 목록, 현재 dependency·버전, UI snapshot, 일반 코딩 스타일, migration 과정은 대개 코드나 온디맨드 문서가 더 낫다.

계속 필요한 기계 규칙은 **먼저** lint·schema·generator·CI로 이전한다. 그 메커니즘이 위반을 저비용으로 확실히 차단한다는 증거가 생긴 뒤에만 상시 지침에서 제거한다. 더 이상 유효하지 않거나 비용 대비 가치가 없는 규칙을 폐기하는 판단과, 유효한 규칙의 집행 위치를 옮기는 판단은 구분한다.

## 3. 색인은 탐색 공간만 줄인다

색인의 기본 해상도는 **디렉터리·패키지·도메인**이다. 목적은 정답 파일을 미리 알려주는 것이 아니라, 검색해야 할 영역을 빠르게 좁히는 것이다.

exhaustive 파일 색인은 검색 기능과 중복되고 구조 변경에 즉시 낡는다. 다만 유일한 정책 SSoT, composition root, 생성물 owner, 승인 manifest처럼 **안정적이고 놓쳤을 때 비용이 큰 파일**은 드문 landmark로 허용한다. 이는 목록이 아니라 잘못된 작성·탐색 시작점을 막는 경계다.

## 4. 설명보다 아키텍처·운영 제약을 남긴다

“현재 무엇이 어디에 있다”는 설명보다 “무엇이 어디를 넘어가면 안 되고, 어떤 작업은 코드 탐색 뒤에 발견하면 늦는가”가 오래 산다.

| 지침에 남길 것 | 다른 SSoT로 보낼 것 |
|---|---|
| owner / must-not-own | 현재 구현 책임과 source layout |
| 허용·금지된 의존 방향 | import와 dependency 목록 |
| trust / security / approval boundary | route·DTO·필드 목록 |
| 하나여야 하는 정책 owner | 현재 symbol·registry 내용 |
| authoring source와 generated output 경계 | 생성물 내용의 수동 복사 |
| 데이터 손실·배포를 막는 필수 gate | 전체 실행 명령 카탈로그 |
| 구현을 가로지르는 lifetime 의미 | 동작 절차와 장애 회고 |

상세 정보의 거처 선택과 이전 원칙은 [컴파운딩 엔지니어링의 정주·SSoT 방법론](./compounding-engineering-learning-harvest.md)을 따른다. 같은 사실을 상시 지침과 상세 문서에 복사하지 않는다.

## 5. 계층형 컨텍스트는 지역화한다

조상 지침은 모든 자손 컨텍스트에 반복 합성된다. 긴 상위 지침은 모든 하위 작업에 붙는 세금이며, 가장 구체적인 leaf 지침이 들어갈 공간을 먼저 소모한다.

- 전역 지침에는 정체성과 전역 불변식만 둔다.
- 공유 규칙은 적용되는 **가장 낮은 공통 조상**에 한 번만 둔다.
- leaf는 부모를 재진술하지 않고 **로컬 차이(delta)**만 추가한다.
- 두 sibling에 같은 의미가 반복되면 공통 부모로 올린다.
- 특정 하위 영역의 예외를 편의상 전역 규칙으로 승격하지 않는다.
- 여러 하네스가 다른 지침 이름을 요구하면 사본 대신 하나의 SSoT를 참조한다.

문자 그대로 같은 문장보다 **의미 중복**을 경계해야 한다. 표현이 달라도 같은 소유권·DI·공개 경계를 여러 계층에서 설명하면 비용과 drift는 그대로 늘어난다.

## 6. 계속 필요한 규칙은 이전 뒤에 제거하고, 검증은 조립과 실제 작업 모두에서 한다

개별 지침이 짧고 올바르더라도 상속 합성 후에는 중복되거나 뒤쪽이 잘릴 수 있다. 일부 하네스는 이를 명시적 오류 없이 처리하므로, 파일 시스템의 doctrine과 모델이 받은 doctrine이 달라진다.

검증은 다음 순서로 한다.

1. 모든 지침, alias, root-to-leaf 상속 순서를 찾는다.
2. 개별 크기보다 **가장 긴 조립 경로**를 기준선으로 잡는다.
3. 각 문장을 `라우팅 / 고위험 불변식 / 자동 집행 후보 / 온디맨드 절차 / 역사`로 분류한다.
4. 현재 owner와 경계를 코드로 확인하되, 확인한 구현 snapshot을 다시 지침에 복사하지 않는다.
5. 상시 지침에서는 빼지만 계속 필요한 규칙의 자동화·문서·live metadata 거처를 먼저 만들고 실제 회수 또는 fail-fast를 확인한다.
6. 위에서 아래로 재작성하고 leaf에는 차이만 남긴다.
7. 대표 leaf의 첫 제목과 마지막 문장이 실제 모델 컨텍스트에 모두 보이는지 확인한다.
8. 조립 크기가 관측 한도에 붙지 않도록 안전 여유를 둔다.
9. alias는 같은 SSoT를 가리키는지, 명시적 제외 범위는 diff가 없는지 확인한다.

조립 검증만으로 engineering 품질이 좋아졌다고 결론 내리면 안 된다. verbose / light / risk-weighted hybrid 조건에서 같은 대표 작업을 반복해 다음을 비교한다.

비교에서는 지침 외 변수를 고정한다. 최소한 모델, 상위 system/developer prompt, tool catalog, 영속 memory·retrieval 정책, reasoning 설정, sandbox, 검색·시간 예산이 같아야 한다. 그렇지 않으면 지침이 짧아진 효과와 다른 컨텍스트가 탐색을 늘린 효과를 구분할 수 없다. 실제 운영 조건을 재현하는 시험과 지침 자체를 분리 측정하는 통제 시험을 나눠 두는 편이 안전하다.

- 조립 토큰과 truncation 여부
- 올바른 owner·SSoT에 도달하기까지의 시간, 검색 호출, 읽은 범위
- task success와 첫 수정의 정확도
- 중대 제약 위반, 리뷰 지적, 재작업 횟수

성공 기준은 task success와 중대 위반이 비열등하고, 각 비용 축의 악화가 사전 허용 범위 안에 있으면서 상시 주입·탐색·재작업·유지보수의 **가중 총비용이 감소하는가**다. 핵심 지표는 corpus 합계 하나가 아니라 최악의 조립 경로와 실제 작업 결과의 결합이다.

## 7. 안티패턴

| 안티패턴 | 실패 모드 |
|---|---|
| **exhaustive 미니 소스 트리** | 구조 변경 즉시 stale해지고 잘못된 위치를 선주입한다 |
| **기능 snapshot** | 현재 UI·API 상태를 장기 architecture invariant로 오인한다 |
| **조상 재진술** | 모든 하위 작업의 비용과 의미 drift를 함께 늘린다 |
| **금지 목록 누적** | 삭제된 과거까지 상시 컨텍스트로 운반한다 |
| **필요 규칙의 선이관 없는 삭제** | 계속 유효한 규칙이 자동화나 온디맨드 거처 없이 사라진다 |
| **파일 크기만 검증** | 합성 순서와 silent truncation을 놓친다 |
| **별칭별 내용 복사** | 이름별 사본이 서로 다른 doctrine으로 갈라진다 |

## 8. 바로 쓰는 정리 프롬프트

```text
이 계층형 에이전트 지침을 상시 로드 런타임 컨텍스트로 감사하라.

1. 각 문장의 반복 주입·유지 비용과 탐색 절감 + 발견 지연 가능성 × 위반 비용을 비교한다.
2. 기본 색인은 디렉터리·도메인 수준으로 두되, 유일하고 안정적인 SSoT·composition root·generated owner 파일은 드문 landmark로 허용한다.
3. 소유권·보안뿐 아니라 생성물 직접 수정 금지, 승인·release gate, 데이터 손실을 막는 운영 불변식도 평가한다.
4. 계속 필요한 기계 규칙은 먼저 lint, schema, generator, CI로 이전하고 실제 fail-fast를 확인한 뒤 제거한다. stale하거나 저가치라 폐기하는 규칙과 집행 위치만 옮기는 규칙을 구분한다.
5. 공유 규칙은 가장 낮은 공통 조상에 한 번만 두고 leaf는 로컬 delta만 남긴다.
6. 현재 구현과 사실을 대조하되 구현 snapshot을 지침에 다시 복사하지 않는다.
7. 변경 전후 최악의 root-to-leaf 조립과 대표 작업의 success, 탐색량, 중대 위반, 재작업을 함께 비교한다. 지침 효과를 볼 때는 모델·상위 prompt·tool·memory·reasoning·sandbox·검색 예산을 통제하고, 실제 운영 재현 시험과 지침 자체의 통제 시험을 나눠 해석한다.
```

핵심은 단순히 **더 적게 쓰는 것**이 아니라 **미리 아는 기대편익이 반복 비용보다 큰 것만 쓰는 것**이다. 목적은 토큰 절약 자체가 아니라, 가장 구체적이고 중요한 경계가 필요할 때 실제로 모델에 도달하면서도 누락 비용을 키우지 않는 데 있다.
