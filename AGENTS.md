# AGENTS.md — ai-insights 저장소 작성 규약

이 저장소는 **일반화된 AI 활용(엔지니어링) 방법론**을 모은다. 에이전트가 여기에서 문서를 작성·수정할 때 아래 규약을 따른다. 사람 독자를 위한 소개·인덱스는 `README.md`에 있고, 작성 규칙의 단일 출처(SSoT)는 이 파일이다.

## 1. 핵심 철학 — 일반화만 수록 (가장 중요)

- 이 저장소의 모든 문서는 **도구·프로젝트·저장소에 종속되지 않는 일반 원리**여야 한다.
- **특정 저장소의 도메인이나 특수화된 정보(제품명·내부 도구명·스킬명·파일경로·API·필드명·특정 수치 등)를 그대로 삽입하지 않는다.** 그런 경험에서 출발하더라도, 반드시 **추상 원리로 풀어서(generalize)** 설명한다.
- 구체 사례가 교육적으로 필요하면, **도구·프로젝트명을 지운 추상 시나리오**로 제시한다. "X 도구의 Y 함정"이 아니라 "직접 주입한 상태가 내부 로직에 덮이는 함정"처럼 *형태*로 기술한다.
- 자가 점검 질문: **"이 문장이 특정 저장소를 아는 사람에게만 의미가 있나?"** 그렇다면 더 풀어 써라.

## 2. 무엇을 담고, 무엇을 담지 않나

- **담는다**: 재사용 가능한 방법론·원리·패턴·프롬프트 전략·가드레일·안티패턴.
- **담지 않는다**: 특정 프로젝트의 셋업·명령어·경로·구성, 1회성 디버깅 기록, 도구 버전 의존 정보, 사내·저장소 한정 컨벤션.
- 판단 기준은 **재발성·비용·일반성** 3조건(자세한 건 수확 방법론 문서 참조). 셋이 모두 서고 *일반화 가능*할 때만 한 문서로 남긴다.

## 3. 작성 규약

- **파일명**: 영문 kebab-case `.md` (예: `compounding-engineering-learning-harvest.md`).
- **구조**: 맨 위 한 줄 **요지(blockquote)** → 원리 → 적용/방법 → 가드레일/안티패턴. 스키머블하게(표·목록 활용).
- **인코딩 원칙**: 결론만 적지 말고 **트리거 + 행동 + 이유**를 함께 — 미래 독자가 맥락 변화 시 재판단할 수 있게.
- **언어**: 한글 본문, 기술 용어는 원형(영문) 유지.
- **최소주의**: 적고 강한 문서 > 많고 약한 문서.

## 4. 유지보수

- **가지치기**: 오래되거나 틀린 문서는 삭제한다. 인코딩과 삭제는 한 쌍이다.
- **중복 금지(SSoT)**: 같은 원리를 두 문서에 나누지 말고 한 곳에 모은다. 겹치면 통합하거나 상호 링크로 단일화한다.
- **검증 선행**: 추측을 일반 원리처럼 박지 않는다. 실제로 성립을 확인한 것만 남긴다.

## 5. 메타

이 저장소 자체가 컴파운딩의 산출물이다 — 여러 작업에서 얻은 학습을 일반 원리로 환류해 다음 작업의 기저선을 올린다. 새 문서를 추가하면 `README.md` 인덱스에 한 줄을 더한다.

> `CLAUDE.md`는 이 파일(`AGENTS.md`)을 가리키는 심볼릭 링크다. 규약은 이 파일에서만 수정한다.
